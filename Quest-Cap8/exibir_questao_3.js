const funcao = require('./funcao_questao_3');

const a = 10;
const b = 5;

console.log(`Soma: ${funcao.soma(a, b)}`);
console.log(`Subtração: ${funcao.subtracao(a, b)}`);
console.log(`Multiplicação: ${funcao.multiplicacao(a, b)}`);
console.log(`Divisão: ${funcao.divisao(a, b)}`);
console.log(`Exponenciação: ${funcao.exponenciacao(a, b)}`);
